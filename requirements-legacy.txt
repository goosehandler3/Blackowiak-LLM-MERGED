# Alternative requirements for older Python versions (3.8-3.9)
# Use this file if you encounter version conflicts with requirements.txt

# Core Audio Processing
openai-whisper==20231117
torch>=1.13.0,<3.0.0
torchaudio>=0.13.0,<3.0.0

# Basic speaker diarization (fallback for older Python)
# pyannote.audio requires Python 3.10+, so we'll use a simpler approach
speechbrain>=0.5.14

# LLM Integration
ollama>=0.1.9
requests>=2.28.0

# Audio File Handling
librosa>=0.9.0
soundfile>=0.12.1
pydub>=0.25.1

# CLI and Utilities
click>=8.0.0
rich>=12.0.0
python-dotenv>=0.19.0

# Privacy & Security (compatible versions)
presidio-analyzer>=2.0.0
presidio-anonymizer>=2.0.0
cryptography>=3.0.0

# Data Processing
pandas>=1.5.0
numpy>=1.21.0
